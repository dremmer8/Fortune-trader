# Firebase Functions Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to Git!

# Admin UIDs (comma-separated, no spaces)
# Get your UID via: firebase.auth().currentUser.uid
# Example: ADMIN_UIDS="abc123xyz456,def789ghi012"
ADMIN_UIDS=
